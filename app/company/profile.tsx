import { View, Text } from 'react-native';

export default function CompanyProfile() {
  return (
    <View style={{ flex: 1, padding: 24 }}>
      <Text style={{ fontSize: 20, fontWeight: '700' }}>Betrieb Profil (kommt als nächstes)</Text>
    </View>
  );
}
